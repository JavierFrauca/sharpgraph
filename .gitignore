# Build output (.NET)
bin/
obj/
publish/
dist/
*.user

# Editor / local
.vs/
.vscode/
.claude/

# Python
__pycache__/
*.pyc

# Benchmark: entradas y salidas específicas del usuario (repos/símbolos privados)
bench/questions.py
bench/RESULTS.md
# Benchmark público: el repo externo a clonar para reproducir, nunca se commitea
bench/_external/

# Documento de comparativa interno (referencias a repos privados)
docs/Comparativa_MCP_CodeGraph_vs_LocalGraph_vs_SinMCP.md

# Logs sueltos de desarrollo
scan_output.txt
test_nopath.txt

# ZCode CLI (archivos locales del agente, no del proyecto)
.zcode/
